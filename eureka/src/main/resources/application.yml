server:
  port: 8761

spring:
  application:
    name: eureka

eureka:
  instance:
    hostname: slave
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: false   # 自己作为client注册自己为false
