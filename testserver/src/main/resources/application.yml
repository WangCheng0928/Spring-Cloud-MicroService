spring:
  application:
    name: testServer
eureka:
  instance:
    hostname: master
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: false
    fetch-registry: false
server:
  port: 8761
